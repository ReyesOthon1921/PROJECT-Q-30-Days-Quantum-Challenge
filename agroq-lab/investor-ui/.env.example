# Leave blank for Vite's local /api proxy.
# For a separately hosted Flask backend, use its HTTPS base URL.
VITE_AGROQ_API_BASE=

# Flask pages used for sign-in, profiles, invitations, and beta access.
VITE_AGROQ_BACKEND_URL=http://127.0.0.1:5000
